version: "3.8"
services:
  # db:
  #   image: postgres
  #   ports:
  #     - "5432:5432"
  web: 
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/task_manager
    ports:
      - "3000:3000"
    # depend_on:
    #   - db
